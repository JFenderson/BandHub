{
  "name": "@hbcu-band-hub/shared-types",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist && find src -name '*.js' -delete && find src -name '*.js.map' -delete && find src -name '*.d.ts' -delete"
  }
}