{
  "name": "@hbcu-band-hub/shared-types",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist && rimraf --glob \"src/**/*.js\" \"src/**/*.js.map\" \"src/**/*.d.ts\""
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}